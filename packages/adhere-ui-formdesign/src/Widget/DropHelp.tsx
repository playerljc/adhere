import React, { memo } from 'react';

import { selectorPrefix } from '../FormDesign/FormDesign';

/**
 * DropHelp
 * @description Drop的辅助线
 * @constructor
 */
const DropHelp = () => <div className={`${selectorPrefix}-drop-help`}></div>;

export default memo(DropHelp);
