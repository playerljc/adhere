import React, { memo } from 'react';
import type { FC } from 'react';

import type { OutlineViewProps } from '../../types/OutlineViewTypes';

const OutlineView: FC<OutlineViewProps> = () => <div>OutlineView</div>;

export default memo(OutlineView);
