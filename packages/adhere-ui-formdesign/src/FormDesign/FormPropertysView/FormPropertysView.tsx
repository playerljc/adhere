import React, { memo } from 'react';
import type { FC } from 'react';

import type { FormPropertysViewProps } from '../../types/FormPropertysViewTypes';

/**
 * FormPropertysView
 * @description 表单属性设计视图
 * @constructor
 */
const FormPropertysView: FC<FormPropertysViewProps> = () => <div>FormPropertysView</div>;

export default memo(FormPropertysView);
