import BackTopAnimation from './BackTopAnimation';
import type { BackTopAnimationProps, ScrollAnimationConfig } from './types';

// 导出组件
export default BackTopAnimation;

// 导出类型
export type { BackTopAnimationProps, ScrollAnimationConfig };

// 导出组件作为命名导出
export { BackTopAnimation };
