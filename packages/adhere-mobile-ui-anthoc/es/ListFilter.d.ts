import React from 'react';
import type { ListFilterProps } from './types';
declare function ListFilter<Option>({ options, filterProps, children, wrapperClassName, wrapperStyle, filterWrapperClassName, filterWrapperStyle, bodyWrapperClassName, bodyWrapperStyle, renderEmpty, }: ListFilterProps<Option>): React.JSX.Element;
export default ListFilter;
