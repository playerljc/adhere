import React from 'react';
import type { UseListFilterProps } from './types';
declare function useListFilter<Option>({ options, filterProps, children, wrapperClassName, wrapperStyle, filterWrapperClassName, filterWrapperStyle, bodyWrapperClassName, bodyWrapperStyle, }: UseListFilterProps<Option>): React.JSX.Element;
export default useListFilter;
