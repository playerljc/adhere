/**
 * isDragEnd
 * @description 是否可以放置
 * @param {string} activeType
 * @param {string} overType
 * @return {boolean}
 */
export declare function isDragEnd({ activeType, overType }: {
    activeType: string;
    overType: string;
}): boolean;
