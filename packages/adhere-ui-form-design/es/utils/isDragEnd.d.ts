/**
 * isDragEnd
 * @description 是否可以放置
 * @param overId
 * @param activeType
 * @param overType
 * @return {boolean}
 */
export declare function isDragEnd({ activeType, overType, }: {
    overId: string;
    activeType: string;
    overType: string;
}): boolean;
