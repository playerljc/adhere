// TimePickerEvents

export const TimePickerEvents = {
  handler: () => {
    return [
      { label: 'onChange', value: 'onChange' },
      { label: 'onOpenChange', value: 'onOpenChange' },
    ];
  },
};
