import type { Use } from './types';
/**
 * use
 * @description use promise
 * @param {Promise} p
 *     data: any;
 *     isPending: boolean;
 *     isValidate: boolean;
 * } result
 * @param defaultArgs
 */
declare const use: Use;
export default use;
