import { ITextStyle } from '../../types';

/**
 * TextStyle
 */
const TextStyle: ITextStyle = {
  font: '10px sans-serif',
  textAlign: 'start',
  textBaseline: 'middle',
  direction: 'inherit',
};

export default TextStyle;
