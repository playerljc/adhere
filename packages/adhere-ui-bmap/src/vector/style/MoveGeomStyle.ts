import { IGeometryStyle } from '../types';

/**
 * 移动的元素样式
 */
const MoveGeomStyle: IGeometryStyle = {
  fillStyle: '#fff',
  // 描边颜色
  strokeStyle: '#fff',
  // 描边大小
  lineWidth: 1,
  lineCap: 'round',
  lineJoin: 'round',
  lineDash: [5, 4, 3],
  lineDashOffset: -1,
  globalAlpha: 0.6,
};

export default MoveGeomStyle;
