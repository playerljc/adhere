import SwipeOut from './SwipeOut';
import type { SwipeOutProps, SwiperRef, SlideStateMap } from './types';

// 导出组件
export default SwipeOut;

// 导出类型
export type { SwipeOutProps, SwiperRef, SlideStateMap };

// 导出组件作为命名导出
export { SwipeOut };
