/** 默认对话框宽度 */
export const DEFAULT_WIDTH = 300;

/** 默认对话框层级 */
export const DEFAULT_ZINDEX = 999;

/** 提示框默认布局配置 */
export const PROMPT_LAYOUT = {
  labelCol: { span: 6 },
  wrapperCol: { span: 18 },
} as const;
