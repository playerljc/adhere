import type { CircularMenuConfig, Point } from '../types';
declare const _default: {
    open(config: CircularMenuConfig, point: Point): void;
    hide(): void;
    styles(properties: object): void;
};
export default _default;
