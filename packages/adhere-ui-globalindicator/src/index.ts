import GlobalIndicator from './GlobalIndicator';

// 导出类型定义
export type {
  GlobalIndicator,
  GlobalIndicatorOptions,
  Size,
  SpinnerConfig,
} from './types';

// 导出默认组件
export default GlobalIndicator;
