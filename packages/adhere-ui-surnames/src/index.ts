import Surnames from './Surnames';

// 导出类型定义
export type {
  SurnamesProps,
  SurnamesRefHandle,
  IndexConfig,
  Record,
  Position,
  Direction,
  EventHandler,
  TouchEventHandler,
  MouseEventHandler,
} from './types';

// 导出默认组件
export default Surnames;
