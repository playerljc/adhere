import ScrollLoad from './ScrollLoad';

// 导出类型
export type {
  ScrollLoadComponent,
  ScrollLoadProps,
  ScrollLoadRefHandle,
  ScrollLoadStatus,
} from './types';

// 导出常量
export { SCROLL_LOAD_STATUS } from './types';

// 导出默认组件
export default ScrollLoad;
